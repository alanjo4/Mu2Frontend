# Config de PRODUCCIÓN del frontend. Vite lo usa automáticamente con `npm run build`.
# En dev (`npm run dev`) se ignora y usa el fallback http://127.0.0.1:3000/api.
VITE_API_BASE_URL=https://api.musyliums.com/api
